# Project State

## Purpose

Beep-get Expo/React Native app with product/visual direction captured in `.brand.json` and mockup files.

## Current Status

- Active Git repo with Expo, Supabase, Jest, and React Native dependencies.
- AI harness files are committed and pushed on `master`.
- Android prebuild and debug Gradle assemble are working locally after SDK 54 dependency alignment and widget module fixes.
- EAS project is linked as `@hypeboyo/beep-get` with project ID `2c41736e-942b-4593-8fcd-53373d03ee53`.
- Android release configuration now targets package `com.hypeboyo.beepget` and Google Play internal testing.
- Backend-free UI preview mode is available behind `EXPO_PUBLIC_UI_PREVIEW=1` for emulator review before Supabase/OAuth are configured.
- Home preview now follows the saved Swiss Paper widget mockup direction instead of the old generic LCD/neumorphic card layout.
- Supabase CLI is installed as a project dev dependency; local Supabase config is initialized for the clean Beep/Blink backend plan.
- Active Supabase migration direction is now v2 `profiles/signals/signal_media/usage_daily`; the old `users/messages/friendships` migrations are archived and should not be pushed to a new project.
- Supabase project `beep-get-prod` is linked locally with project ref `dyuzxilukcwiavtvbmci`; v2 Beep/Blink migration has been pushed to the remote database through the IPv4 pooler path.
- Local `.env` is configured with the remote Supabase URL and public/publishable app key; secret DB password remains gitignored under `supabase/.temp/`.
- Beep/Blink domain limits and presentation payload helpers are implemented with tests, including the 2-second Blink cap and widget-safe teaser payloads.
- Home preview now acts as a Today/Reply Room entry point for the Beep/Blink loop, and `ReplyRoom` is available as a modal/deep-link target.
- Runtime services now target the v2 Supabase schema: auth uses `profiles/create_profile`, friends use `relationships/find_profile_by_beep_id`, Beeps use `signals/send_beep`, dictionary entries use `code_presets`, skin/status updates use `profiles`, and Realtime listens on `signals`/`profiles`.
- Legacy UI shapes are preserved through adapters so existing screens and widget sync can keep using `from_user`, `number_code`, `is_read`, and `user_id` while the backend stores `sender_id`, `code`, `status`, and `owner_id`.
- Blink media storage now has a test-covered adapter boundary that validates 2-second/750KB uploads, creates `create_blink_metadata` rows, returns signed Supabase upload targets, and issues short-lived playback references without permanent public URLs.
- App-side usage guardrails now have a pure evaluator for daily Beep/Blink limits and per-relationship Blink cooldowns before the UI calls paid/storage-heavy paths.
- Blink send now has an app-side capture/send path: `SendScreen` can switch between Beep and Blink, records a 2-second muted camera clip through `expo-camera`, uploads through the signed Supabase Storage target, and finalizes media with `finalize_blink_upload`.
- The `finalize_blink_upload` RPC has been pushed to `beep-get-prod`; it only lets the authenticated sender mark their own pending Blink media as `uploaded`.
- The approved UX direction is now the Swiss Paper "slip" model: widget as `Incoming Slip`, Send screen as `Outgoing Slip`, and Reply Room as `Signal Detail + Quick Reply`.
- `DESIGN.md` is now the implementation source of truth for the app UI redesign; the downloaded starter kit has been integrated into the app-level visual system, components, and mock-data screens without native widget implementation.
- Main app navigation now opens the slip-first primary tabs: Today, People, Send, Studio, and Logs, with Reply Room and widget states available as modal/deep-link targets.
- Android emulator visual QA refined the slip UI shell: pager screens now respect the status-bar safe area, primary tabs stay to five one-line labels, and the Send Beep/Blink switcher lives inside the pager surface.
- Base stage/shell colors are neutral black/charcoal; avoid warm near-black values such as `#11110F` because they read as mugwort/olive on Android emulator screenshots.
- Login now uses a frameless cream paper-slip surface with the approved concept-1 Beepy pager mascot; the production mascot is a handdrawn PNG asset at `assets/brand/beepy-handdrawn.png`, not the earlier clean code-vector drawing. Do not add a literal phone/iPhone frame inside the app UI.
- Runtime app screens now use a frameless cream `AppSurface` instead of the black `PagerFrame` hardware shell; `PagerFrame` should stay reserved for non-runtime mockup/widget demonstration contexts.
- Platform auth CTAs now show Google, Apple, and Kakao in that order. iOS keeps native Apple sign-in, while non-native Apple fallback, Google, and Kakao use Supabase OAuth with the existing PKCE deep-link callback.
- iOS native Apple login now includes the nonce pair expected by Supabase native ID-token auth: the app sends a SHA-256 hashed nonce to `expo-apple-authentication` and the raw nonce to `supabase.auth.signInWithIdToken`, with a no-SubtleCrypto fallback covered by tests.
- Direct widget preset replies are now considered required product behavior for the strong widget loop; implementation should start with latest-signal preset Beep replies, then add idempotency before native direct-send actions ship.
- Android widget action URLs now cover open reply room, confirm, save, and preset quick reply; the app handles these deep links and preview mode syncs native widget sample data after entering UI Preview.
- The `DESIGN.md` slip screens are now wired to live app state instead of mock-only starter data: Today reads received Beep/Blink signals, People reads and adds relationships, Send uses the Beep/Blink send services, Reply Room fetches cold deep-link signals and sends quick replies, Logs reads saved signals, and Studio syncs widget data plus live reply slots.
- App-side widget/reply quick replies are idempotent end-to-end through the remote `reply_with_preset_once` RPC on `beep-get-prod`; duplicate widget/app taps reuse the same actor/client-action reservation instead of creating duplicate Beeps.
- OAuth is implemented with Supabase PKCE callback exchange through `expo-linking`; Google, non-native Apple fallback, and Kakao share the same callback path, while provider secrets/dashboard settings remain external configuration, not commit-safe repo changes.
- `package-lock.json` has been security-refreshed with `npm audit fix`; the remaining audit findings are low/moderate Expo/Jest transitive issues whose suggested fix requires breaking major-version force changes.
- Android Glance widgets are moving from the old green LCD look toward the Swiss Paper slip style; the medium widget includes `OK / 8282 / OPEN` action chips for home-screen QA.
- Android Glance widget colors must use Compose `Color(...)` values inside `ColorProvider`; Android `Color.parseColor(...)` ints are treated as resource IDs by RemoteViews hosts and can show `Can't load widget`.
- Android small Glance widget now renders latest Blink payloads as `Incoming Blink` with an image thumbnail/strip preview and updates through `GlanceAppWidget.updateAll(...)` instead of shell-blocked/stale app-widget broadcasts.
- Android widget setup and action handling now treat Android as first-class: the in-app Widget Setup screen gives launcher-widget instructions on Android, widget action URLs are deferred until app navigation/auth/profile are ready, and completed confirm/save/quick-reply actions route into Reply Room.
- The approved before/after UI/UX polish pass is implemented in app code: HeaderBar no longer exposes inert side controls, Send Beep/Blink use real `BACK` and `LOGS` actions, bottom tabs are larger with an active indicator, People uses `CLOSE FRIEND`, and UI Preview Blink shows `MOCK CAMERA / PREVIEW BLINK` instead of implying camera permission is mandatory.
- Metro can now honor `METRO_MAX_WORKERS` from `metro.config.js` so Windows/sandbox QA can force single-worker bundling when child-process spawning hits `EPERM`.
- Production Blink sends now generate a real 3-frame private JPEG teaser strip with `expo-video-thumbnails`, upload original video and teaser frames through signed Supabase Storage URLs, and hydrate read-side Blink media with short-lived signed URLs.
- iOS WidgetKit small/medium layouts now use dedicated full-canvas Swiss Paper ticket layouts with disabled widget content margins, widget container backgrounds, expanded 3-slot Blink preview strips, and app-owned `beepget://reply/...` open links that defer until navigation/auth/profile are ready.
- Blink sending now uses a two-phase app flow: first capture a 2-second video and render its generated three-frame preview, then send the captured video plus the same teaser assets. Read-side teaser frame signed URLs are longer-lived than playback URLs so widget previews do not expire immediately.
- The production IA cleanup pass removes duplicate/inert surfaces from the primary tabs: Today keeps latest signal, quick reply, and queue only; People uses one Close Circuit list instead of a separate Widget Circle; MY hides Studio and Collection from user-facing room tools while those product concepts remain undecided.
- Remote `beep-get-prod` migrations are deployed through `20260507110000_demote_policy_helpers.sql`; remote schema includes Blink teaser strip keys, private `blink-originals`/`blink-thumbs` policies, `get_own_profile()`, minimum authenticated table grants, no anon public-table grants, and invoker-mode RLS helper functions.
- Android Google OAuth session persistence has been re-smoked: the existing Supabase session survived app restart, `get_own_profile()` hydrated the profile, and the app opened the live Today tab instead of returning to nickname onboarding.
- Account controls are now visible in the app under the `ME` tab: share Beep ID, log out, privacy policy, web deletion request, and in-app account deletion.
- Account deletion is implemented through the deployed Supabase Edge Function `delete-account`; it verifies the authenticated user, removes Blink Storage objects, deletes the Supabase Auth user, and records a hashed deletion request audit row without retaining raw user ID after completion.
- Remote `beep-get-prod` migrations are deployed through `20260507123000_account_deletion_requests.sql`; the remote database dry-run reports up to date.
- Approved next product direction is captured in `docs/plans/2026-05-08-four-tab-identity-shop.md`: reduce primary navigation to `TODAY / SEND / PEOPLE / MY`, make `MY` the widget/personalization/account room, support short text reply slots, and frame monetization as official identity packs rather than seasonal scarcity.
- Approved follow-up UX direction is captured in `docs/plans/2026-05-10-today-send-people-emoji-design.md`: keep `TODAY / SEND / PEOPLE / MY`, strengthen Today as Signal Desk, Send as Signal Deck, People as Close Circuit, and move paid-quality Beepy emotes toward real image assets instead of coded placeholder doodles.
- The Today/Send/People follow-up patch is applied: Today now renders Signal Desk sections, Send has a Signal Deck header/slot/recent-combo structure, People has My Beep ID/Close Circuit/Invite Slip sections, identity-pack metadata now has a real Beepy emote asset boundary under `src/design/identityPacks.ts` plus `assets/brand/emotes/README.md`, and local code now allows short text signal tokens pending remote migration `20260510150000_allow_text_signal_tokens.sql`.
- The GPT Image style-shop direction is now translated into an app-native `MY BEEP ROOM`: profile slip, current friend-home widget preview, selectable official identity-pack cards, selected-pack detail, reply slots, shared Blink 3-cut preview, and account/tool links. Avoid reintroducing the earlier black editorial poster board as a runtime app screen. The separate LCD/Pixel pack has been removed because that visual territory overlaps with `Night Signal`.
- Primary navigation now presents the four product tabs `TODAY / SEND / PEOPLE / MY`; logs, old Studio tooling, account controls, and widget-state previews remain reachable from `MY` as stack/modal routes.
- `MY` style-shop QA now keeps pack detail tied to the selected skin: tapping an identity pack scrolls directly to the detail card, detail surfaces inherit each pack tone/decor, Blink 3-cut frame labels render reliably, and optional room tools are compacted into a 2x2 grid.
- `MY` style-shop picker now avoids the earlier dense two-column card wall: selected pack context is shown in a single `NOW PREVIEWING` panel, pack choices are compact ticket rows, and Blink 3-cut pack art uses skin-specific mini marks instead of placeholder `IMG` labels.
- `MY` paid-pack previews now avoid blank black media strips: Photo Booth Blink uses camera/Beepy/pose emote chips and a visible three-cut photo strip, while other packs render tone-matched mini art instead of raw text labels.
- `MY` identity-pack previews now use a consistent purchasable preview grammar: each selected pack detail shows three themed Beepy emote chips and a tone-specific Blink 3-cut strip instead of color-only variants.
- Beepy/pack emote art direction now has a standalone source-of-truth document at `docs/plans/2026-05-10-beepy-emote-design.md`; the default `Classic Paper` skin uses the canonical hand-drawn Beepy mascot, while paid packs must use skin-native emote subjects instead of Beepy costume/action variants.
- Beepy emote asset v2 is imported into the app: five packs have 512px transparent PNG assets under `assets/brand/emotes/`, `src/design/beepyEmoteAssets.generated.ts` provides static `require(...)` references, `identityPacks` hydrates expressions as `source: "asset"`, and `MY` pack detail renders those imported images before falling back to coded placeholders.
- Blink preview fallbacks now use one shared full-color GPT Image person strip framed as a 2-second Blink sample: `assets/brand/blink/blink-person-model-strip.png` is wired through `src/design/blinkPersonModel.ts`, and `MY` plus generic `BlinkStrip` fallbacks render timestamped `0.0s / 0.7s / 1.3s` extracted-frame art instead of abstract `B`/`IMG` placeholders.
- macOS/iOS availability may block iOS verification.

## Next Work Queue

- Add production EAS env values for `EXPO_PUBLIC_SUPABASE_URL` and `EXPO_PUBLIC_SUPABASE_ANON_KEY`.
- Enable the Kakao provider in Supabase for `beep-get-prod` once Kakao REST API key/client secret are available; `npm run kakao:supabase-provider` is wired for the Management API path.
- Confirm real authenticated Android camera/file upload against Supabase Storage with a non-preview sender/receiver pair.
- Publish real privacy policy and account deletion web request URLs before store submission; current app code supports `EXPO_PUBLIC_PRIVACY_URL` and `EXPO_PUBLIC_ACCOUNT_DELETION_URL`.
- Decide whether v2 should reintroduce collection/season/icon rewards; production collection/season services currently return empty safe fallbacks because those tables are intentionally absent from the v2 migration.
- Configure Google Play Console/service account, then run production EAS build and submit.
- Keep visual changes aligned with `.brand.json` and existing mockups.
- Continue UI/UX review from the current `EXPO_PUBLIC_UI_PREVIEW=1` emulator build for real-device pacing and medium-widget placement.
- Continue polishing paid identity-pack assets against `docs/plans/2026-05-10-beepy-emote-design.md` before adding real IAP; preview grammar is consistent now, but final commissioned sticker art is still pending and must avoid same-mascot costume/action variants.
- Add the reply-slot editor and persistence rules from `docs/plans/2026-05-08-four-tab-identity-shop.md`; current `MY` uses existing slots plus safe defaults.
- Keep identity packs as official mock/locked packs until StoreKit / Google Play Billing is explicitly designed and implemented.
- Add native background widget direct-send actions only after native auth/session sharing and retry UX are deliberately designed; the current safe implementation routes widget taps through app-owned deep links backed by server idempotency.
- Prefer Android/web/Jest checks when iOS cannot be verified locally.
- Avoid broad app rewrites while platform verification is limited.

## Known Blockers

- Android emulator QA is available, but Pixel Launcher widget placement is manual because `adb shell cmd appwidget` reported `No shell command implementation.`
- EAS production environment has the public Supabase URL/anon key configured; provider secrets and dashboard auth-provider settings still live outside git. Kakao provider enablement still needs Kakao app credentials outside the repo.
- Google Play service account credentials are not configured in the repo and must not be committed.
- Collection/season/icon rewards are preview-only until a v2 rewards schema is designed.
- Status labels are UI-only for now; v2 persists `profiles.status_icon`, not separate `status_broadcasts.label`.
- Blink upload adapter, private signed-upload policy shape, and SendScreen preview UI are verified, but a real two-account Android camera upload/finalize against Supabase Storage still needs runtime QA.
- iOS verification may require macOS.
- Apple OAuth provider dashboard setup cannot be completed from repo code alone; keep provider secrets, Apple service IDs, and redirect allow-lists out of git.
- The destructive account deletion flow should only be end-to-end tested with a disposable authenticated account; do not invoke it from a real user session.

## Last Verified

- 2026-05-25: Blink capture-preview/send flow and production IA cleanup on branch `codex/ios-widget-layout-blink-preview`: focused Blink/Today/People/MY/message tests passed, `npm.cmd run typecheck` passed, full `npm.cmd test -- --runInBand` passed 281 tests, `npx.cmd --yes expo-doctor` passed 18/18, `git diff --check` passed with CRLF warnings only, and `npx.cmd expo export --platform ios` passed. Windows still lacks Swift/Xcode, so native iOS widget compilation and real-device widget placement need EAS or macOS.
- 2026-05-25: iOS WidgetKit full-canvas small/medium layout and Blink demo/open-link pass on branch `codex/ios-widget-layout-blink-preview`: `npm.cmd run typecheck` passed, full `npm.cmd test -- --runInBand` passed 276 tests, `npx.cmd --yes expo-doctor` passed 18/18, `npx.cmd expo config --type prebuild` showed both iOS app extensions, `git diff --check` passed with CRLF warnings only, and `npx.cmd expo export --platform ios` passed. Windows still lacks Swift/Xcode, so native iOS widget compilation needs EAS or macOS.
- 2026-05-02: EAS project linked; `npx expo-doctor` passed 17/17, `npm run typecheck` passed, `npm test -- --runInBand` passed 181 tests, `npx expo prebuild --platform android --no-install` completed, `android/gradlew.bat -p android :app:assembleDebug --console=plain` built successfully, and `android/gradlew.bat -p android :app:bundleRelease --console=plain` built a release AAB successfully after adding missing font assets.
- 2026-05-03: Beep/Blink domain/presentation and Today/Reply Room preview work added; `npm run typecheck` passed, `npm test -- --runInBand` passed 198 tests, and `npx expo-doctor` passed 17/17. `npm run supabase:lint` could not rerun because Docker Desktop was not running after the local stack had been stopped.
- 2026-05-03: Remote Supabase `beep-get-prod` migration push succeeded after relinking with the DB password and IPv4 pooler path; `supabase db lint --linked` reported no schema errors and remote table stats showed the v2 Beep/Blink tables.
- 2026-05-03: App service layer was rewired to the v2 Supabase schema; `npm run typecheck` passed, `npm test -- --runInBand` passed 169 tests, `npx expo-doctor` passed 17/17, and `supabase db lint --linked` reported no schema errors.
- 2026-05-03: Blink media storage adapter and usage limit evaluator were added; `npm run typecheck` passed, `npm test -- --runInBand` passed 187 tests, `npx expo-doctor` passed 17/17, and `supabase db lint --linked` reported no schema errors after linking the worktree to `beep-get-prod`.
- 2026-05-03: Blink Send UI and upload-finalize path added; `npm run typecheck` passed, `npm test -- --runInBand` passed 194 tests, `npx expo-doctor` passed 17/17, `supabase db push --dry-run --password ...` reported only `20260503050000_finalize_blink_upload.sql`, `supabase db push --password ...` applied it, a follow-up dry-run reported the remote database up to date, and `supabase db lint --linked` found no schema errors.
- 2026-05-03: Android runtime smoke passed on `emulator-5554`: `npx expo prebuild --platform android --no-install` completed, `:app:assembleDebug` passed from short path `C:/bg-blink`, x86_64 debug APK installed after emulator space cleanup, Metro bundled successfully with `EXPO_PUBLIC_UI_PREVIEW=1`, UI Preview entered Home, and the Send screen showed the Beep/Blink switch plus Blink capture panel. Screenshots: `C:/Users/slime/AppData/Local/Temp/beep-get-ui-preview-home.png`, `C:/Users/slime/AppData/Local/Temp/beep-get-blink-send-screen.png`.
- 2026-05-03: Downloaded `DESIGN.md` UI starter integrated into the existing Expo app navigation as Today/People/Send/Studio/Logs/Widget tabs plus slip Reply Room modal; `npx expo-doctor` passed 17/17, `npm run typecheck` passed, and `npm test -- --runInBand` passed 194 tests.
- 2026-05-03: Android emulator visual QA for the integrated slip UI passed after safe-area/tab/Send switcher polish; screenshots saved at `C:/Users/slime/AppData/Local/Temp/beep-get-slip-ui-polished-today.png`, `C:/Users/slime/AppData/Local/Temp/beep-get-slip-ui-polished-send-v2.png`, and `C:/Users/slime/AppData/Local/Temp/beep-get-slip-ui-polished-studio.png`.
- 2026-05-03: Shell color cast fix verified on `emulator-5554`; `src/design/tokens.ts` stage/shell colors were neutralized, screenshot saved at `C:/Users/slime/AppData/Local/Temp/beep-get-neutral-shell-today.png`, `npm run typecheck` passed, `npm test -- --runInBand` passed 194 tests, and `npx expo-doctor` passed 17/17.
- 2026-05-03: Login mascot/platform-auth pass verified on `emulator-5554`; Android screenshot saved at `C:/Users/slime/AppData/Local/Temp/beep-get-login-mascot-android-v2.png`, visual verdict scored 92/pass in `.omx/state/login-mascot/ralph-progress.json`, `npm run typecheck` passed, `npm test -- --runInBand` passed 197 tests, and `npx --yes expo-doctor` passed 17/17. iOS Apple-only runtime verification is still pending on macOS.
- 2026-05-03: Beepy mascot was revised from a clean code-vector drawing to a handdrawn ink-textured PNG asset to match mockup A more closely; Android screenshot saved at `C:/Users/slime/AppData/Local/Temp/beep-get-handdrawn-beepy-login-android.png`, and local visual verdict scored 94/pass in `.omx/state/login-mascot/ralph-progress.json`.
- 2026-05-03: Runtime app screens were moved off the black `PagerFrame` hardware shell onto a cream `AppSurface`; Android UI preview screenshots saved at `C:/Users/slime/AppData/Local/Temp/beep-get-frameless-today-v3.png` and `C:/Users/slime/AppData/Local/Temp/beep-get-frameless-send.png`, with local visual verdict scored 94/pass in `.omx/state/remove-app-pager-frame/ralph-progress.json`.
- 2026-05-03: Widget quick-reply app path and Android Glance slip styling added; `npm run typecheck` passed, `npm test -- --runInBand` passed 204 tests, `android/gradlew.bat -p android :app:assembleDebug --console=plain --no-daemon` passed, widget providers were visible in `dumpsys appwidget`, preview widget shared preferences included `beepget://signal/preview-message-1/quick-reply/8282`, and that deep link smoked on `emulator-5554` without AndroidRuntime crashes. Actual launcher widget placement still needs manual QA because programmatic widget binding was unavailable on the emulator.
- 2026-05-03: Actual Android launcher small-widget placement smoke passed after fixing Glance `ColorProvider` values to use Compose colors; x86_64 debug APK installed on `emulator-5554`, UI Preview synced widget data, Pixel Launcher placed the `BeepWidgetReceiver`, screenshot saved at `C:/Users/slime/AppData/Local/Temp/beep-get-widget-visible-fixed.png`, and logcat no longer reported the previous `Resource ID #0xfff4efe5` RemoteViews color failure.
- 2026-05-04: Android launcher small-widget Blink teaser strip verified on `emulator-5554`; `npm run typecheck` passed, `npm test -- --runInBand` passed 205 tests, `npx --yes expo-doctor` passed 17/17, `:app:assembleDebug -PreactNativeArchitectures=x86_64` passed, UI Preview synced `kind:"blink"` with 3 strip frame URIs, Pixel Launcher showed `Incoming Blink` plus `01   02   03`, screenshot saved at `C:/Users/slime/AppData/Local/Temp/beep-get-widget-three-frame-strip.png`, and post-sync logcat showed no Glance/RemoteViews truncation errors.
- 2026-05-04: Android launcher small-widget Blink thumbnail preview verified on `emulator-5554`; `npm run typecheck` passed, `npm test -- --runInBand` passed 206 tests, `:beep-widget:compileDebugKotlin` passed, `:app:assembleDebug -PreactNativeArchitectures=x86_64` passed, Pixel Launcher showed `Incoming Blink` with an actual preview image, screenshot saved at `C:/Users/slime/AppData/Local/Temp/beep-get-widget-thumbnail-home.png`, and UI tree exposed `Blink preview` as an ImageView.
- 2026-05-04: Slip functional completion pass wired the `DESIGN.md` app screens to live v2 services/stores, added PKCE OAuth callback exchange, added idempotent quick-reply RPC migration `20260504013000_idempotent_preset_replies.sql`, pushed that migration to `beep-get-prod`, and completed code/design/security review. Verification: `npm run typecheck` passed, `npm test -- --runInBand` passed 214 tests, `npx --yes expo-doctor` passed 17/17, `npm audit --audit-level=high` passed with only low/moderate remaining, `supabase db lint --linked` found no schema errors, `supabase db push --dry-run --linked` reported remote database up to date, `git diff --check` passed aside from CRLF warnings, and `C:/bg-slip/android/gradlew.bat -p C:/bg-slip/android :app:assembleDebug -PreactNativeArchitectures=x86_64 --console=plain --no-daemon` built successfully.
- 2026-05-05: Widget States modal escape path verified on `emulator-5554`; `npm.cmd run typecheck` passed, `npm.cmd test -- --runInBand` passed 214 tests, `agent-device` UI Preview opened Studio, `SMALL` opened `WIDGET STATES`, the new `CLOSE` header control returned to Studio, and screenshots were saved under `C:/Users/slime/AppData/Local/Temp/beepget-agent-device-qa/5554-widgetstates-*.png`.
- 2026-05-02: PR #6 merged after CI `validate` passed; `EXPO_PUBLIC_UI_PREVIEW=1` Android release installed on `emulator-5554`, UI preview entered successfully, and screenshot QA confirmed the Swiss Paper home preview is foreground at `C:/Users/slime/AppData/Local/Temp/beep-get-swiss-home-v3.png`.
- 2026-04-30: `npm test -- --runInBand` passed.
- 2026-05-05: UI/UX polish code pass verified with `npm.cmd run typecheck`, `npm.cmd test -- --runInBand` (214 tests), `npx.cmd --yes expo-doctor` (17/17), and `git diff --check` (CRLF warnings only). Android latest-JS screenshot QA on `emulator-5554` was attempted but blocked in this sandbox by Expo CLI `spawn EPERM`/Gradle timeout while producing an embedded QA APK; the emulator was restored to an installed release APK instead of leaving the red dev error screen.
- 2026-05-05: Latest-JS Android UI Preview QA resumed on `emulator-5554` after removing the stale release APK. A signed debug APK with the generated JS bundle installed successfully, the current frameless cream login opened, UI Preview entered Today, Reply Room `BACK` returned to Today, Send `BACK` returned to Today, Send `LOGS` opened Logs, Blink preview showed `MOCK CAMERA / PREVIEW BLINK`, People showed `CLOSE FRIEND`, Studio `SMALL` opened `WIDGET STATES`, and `CLOSE` returned to Studio. Screenshots and UI dumps are under `C:/Users/slime/AppData/Local/Temp/beepget-goal-qa/`.
- 2026-05-05: Verification rerun for the iOS-excluded Android/code QA goal: `npm.cmd run typecheck` passed, `npm.cmd test -- --runInBand` passed 214 tests, `npx.cmd --yes expo-doctor` exited 0, `npm.cmd audit --audit-level=high` exited 0 with only low/moderate Expo/Jest transitive findings that require breaking `--force` fixes, `git diff --check` passed, and Android logcat crash buffer stayed empty after the emulator flow. Full `:app:assembleDebug -PreactNativeArchitectures=x86_64` still timed out in this sandbox at `:app:buildCMakeDebug[x86_64]`, so the installed QA artifact used the already-built debug APK plus freshly generated bundle.
- 2026-05-06: Google OAuth callback was configured and verified against `beep-get-prod` on `emulator-5554`. Supabase authorize returned Google with client ID `887531606879-5onh1rqqq0dongjp4ci1eg0do8r6gnt5.apps.googleusercontent.com`, Google consent redirected back to `beepget://auth/callback`, the app exchanged the PKCE code into a Supabase session, and the Auth screen advanced to the `NICKNAME` profile creation state. The installed QA APK was patched from the existing debug APK with a freshly embedded JS bundle because Gradle `:app:assembleDebug` still stalls at `:app:buildCMakeDebug[x86_64]` in this sandbox. Verification: `npm.cmd run typecheck` passed, targeted auth/Supabase client tests passed, full `npm.cmd test -- --runInBand` passed 217 tests, `npx.cmd --yes expo-doctor` passed 17/17, `git diff --check` passed with CRLF warnings only, and screenshot evidence is at `C:/Users/slime/AppData/Local/Temp/beepget-oauth-pkce-verified-nickname.png`.
- 2026-05-07: Production Blink teaser/storage and Supabase hardening pass deployed to `beep-get-prod`. Remote migrations through `20260507110000` are applied and dry-run reports up to date; `supabase db lint --linked` found no schema errors; performance advisor found no issues; security advisor remaining warnings are the intentional authenticated product RPCs plus dashboard-only leaked-password protection. Android `emulator-5554` installed the latest embedded-JS debug APK, restored the persisted Google/Supabase session after force-stop, opened Today without auth/profile alerts, navigated People/Send/Studio/Logs/Widget States, and crash buffers stayed empty. Evidence is under `C:/Users/slime/AppData/Local/Temp/beepget-production-goal-qa/`. Verification: `npm.cmd run typecheck` passed, `npm.cmd test -- --runInBand` passed 225 tests, `npx.cmd --yes expo-doctor` passed 17/17, `npm.cmd audit --audit-level=high` exited 0 with only low/moderate Expo/Jest transitive findings, `npx.cmd expo prebuild --platform android --no-install` passed, and `android/gradlew.bat -p android :app:assembleDebug -PreactNativeArchitectures=x86_64 --console=plain --no-daemon` passed.
- 2026-05-07: Account/settings and account deletion pass completed on branch `codex/settings-account-deletion`. Remote `beep-get-prod` migration `20260507123000_account_deletion_requests.sql` is applied, `supabase db push --dry-run --linked` reports up to date, and Edge Function `delete-account` is deployed. Verification: `supabase db lint --linked` passed, `npm.cmd run typecheck` passed, `npm.cmd test -- --runInBand` passed 227 tests, `npx.cmd --yes expo-doctor` passed 17/17, `npm.cmd audit --audit-level=high` exited 0 with only low/moderate remaining, Android emulator `emulator-5554` showed the new `ME` account screen with `LOG OUT`, `WEB DELETE REQUEST`, and `DELETE ACCOUNT`, and crash buffer stayed empty. Screenshot: `C:/Users/slime/AppData/Local/Temp/beep-get-account-settings-current.png`.
- 2026-05-09: GPT Image-matched `MY` style shop pass implemented on branch `codex/gpt-image-style-shop`. Verification: `npm.cmd run typecheck` passed, `npm.cmd test -- --runInBand` passed 229 tests, `npx.cmd --yes expo-doctor` passed 17/17, `git diff --check` passed with CRLF warnings only, Android debug APK installed on `emulator-5554`, `MY` tab rendered the black style-shop room with friend-home-screen try-on and identity pack cards, and screenshot evidence was saved at `C:/Users/slime/AppData/Local/Temp/beep-get-my-style-shop.png` and `C:/Users/slime/AppData/Local/Temp/beep-get-my-style-shop-cards.png`.
- 2026-05-09: Follow-up `MY BEEP ROOM` UX correction replaced the over-literal style-shop poster with an app-native personalization flow on branch `codex/my-room-style-shop-ux`. Verification: `npm.cmd run typecheck` passed, `npm.cmd test -- --runInBand` passed 229 tests, `npx.cmd --yes expo-doctor` passed 17/17, `npx.cmd expo export:embed --platform android --dev false --entry-file index.js --bundle-output android/app/src/main/assets/index.android.bundle --assets-dest android/app/src/main/res` passed, `android/gradlew.bat -p android :app:assembleDebug -PreactNativeArchitectures=x86_64 --console=plain --no-daemon` passed, Android debug APK installed on `emulator-5554`, `MY` rendered the corrected profile/current-widget/style-shop/pack-detail flow, app error logcat was clean, and screenshots were saved at `C:/Users/slime/AppData/Local/Temp/beep-get-my-room-ux-v2.png`, `C:/Users/slime/AppData/Local/Temp/beep-get-my-room-style-shop-v2.png`, and `C:/Users/slime/AppData/Local/Temp/beep-get-my-room-detail-v2.png`.
- 2026-05-10: `MY` style-shop QA polish fixed selected-pack detail feedback and density: tapping packs auto-scrolls to detail, non-night detail cards now follow pack skin tone/decor, `BLINK 3-CUT` frame labels stay visible, and `ROOM TOOLS` is a compact optional grid. Verification: `npm.cmd run typecheck` passed, `npm.cmd test -- --runInBand` passed 230 tests, `npx.cmd --yes expo-doctor` passed 17/17, `npx.cmd expo export:embed --platform android --dev false --entry-file index.js --bundle-output android/app/src/main/assets/index.android.bundle --assets-dest android/app/src/main/res` passed, `android/gradlew.bat :app:assembleDebug -PreactNativeArchitectures=x86_64 --console=plain --no-daemon` passed, Android debug APK installed on `emulator-5554`, crash buffer stayed empty, and screenshots were saved under `.omx/state/my-style-shop-qa/`.
- 2026-05-10: Follow-up `MY` style-shop UX QA on branch `codex/my-style-shop-picker-polish` replaced the dense pack card grid with compact ticket rows, added a selected-pack summary panel, and changed Blink 3-cut pack art from `IMG` placeholders to tone-specific mini marks. Verification: `npm.cmd run typecheck` passed, `npm.cmd test -- --runInBand` passed 230 tests, `npx.cmd --yes expo-doctor` passed 17/17, Pixel_7 emulator dev-client QA opened `MY`, pack taps updated selection and auto-scrolled to detail, room tools remained reachable, and evidence was saved under `.omx/state/my-style-shop-picker/`.
- 2026-05-10: `MY` pack-art correction on branch `codex/fix-style-shop-pack-art` fixed the Photo Booth Blink detail preview so `BEEPY EMOTE` renders designed chips instead of text labels and `BLINK 3-CUT` shows visible photo-booth frames instead of a blank black strip. Verification: `npm.cmd run typecheck` passed and Pixel_7 emulator dev-client QA captured the corrected Photo Booth detail at `.omx/state/style-shop-pack-art/photo-detail-fixed.png`.
- 2026-05-10: Style-shop preview consistency pass on branch `codex/standardize-style-pack-previews` standardized pack details around themed emote chips plus tone-specific Blink 3-cut art for Classic, School, Cherry, Photo, and Night. Verification: `npm.cmd run typecheck` passed, `npm.cmd test -- --runInBand` passed 230 tests, `npx.cmd --yes expo-doctor` passed 17/17, Android export/embed and `:app:assembleDebug` passed, emulator tap QA covered all five pack details, and the crash buffer stayed empty.
- 2026-05-10: Today/Send/People/emoji patch applied on branch `codex/apply-today-send-people-emoji-patch`, then hardened so text slots do not break real sends. Verification: focused `npm.cmd test -- --runInBand messageService dictionaryService widgetActions rootNavigator SignalSlotRail identityPacks TodayScreen SendSignalScreen PeopleScreen` passed, `npm.cmd run typecheck` passed, full `npm.cmd test -- --runInBand` passed 237 tests, `npx.cmd --yes expo-doctor` passed 17/17, `git diff --check` passed with CRLF warnings only, `npx.cmd supabase db push --dry-run --linked` reported only `20260510150000_allow_text_signal_tokens.sql` would be pushed, Android export/embed passed, `android/gradlew.bat -p android :app:assembleDebug -PreactNativeArchitectures=x86_64 --console=plain --no-daemon` passed, Pixel_7 emulator showed Today Signal Desk, Send no-friend fallback, and People Close Circuit/My Beep ID screens, and the crash buffer stayed empty.
- 2026-05-10: Beepy/pack emote direction corrected on branch `codex/fix-emote-skin-direction`: `Classic Paper` is the only canonical Beepy mascot family, paid packs are marked as skin-native emote families, the default pack preview uses the hand-drawn Beepy mascot asset, and Photo/Cherry preview placeholders no longer reuse the mini Beepy stand-in. Verification: `npm.cmd test -- --runInBand identityPacks` passed, `npm.cmd run typecheck` passed, full `npm.cmd test -- --runInBand` passed 238 tests, `npx.cmd --yes expo-doctor` passed 17/17, and `git diff --check` passed with CRLF warnings only.
- 2026-05-10: Beepy emote asset v2 import pass wired real PNG emotes into `identityPacks` and `MY` pack detail on branch `codex/import-beepy-emote-assets-v2`; bundle assets were copied without replacing the repo README/design source. Verification: `npm.cmd test -- --runInBand identityPacks` passed, `npm.cmd run typecheck` passed, full `npm.cmd test -- --runInBand` passed 239 tests, `npx.cmd --yes expo-doctor` passed 17/17, `git diff --check` passed with CRLF warnings only, Android export/embed copied 60 assets successfully, and `android/gradlew.bat -p android :app:assembleDebug -PreactNativeArchitectures=x86_64 --console=plain --no-daemon` passed.
- 2026-05-10: Blink 3-cut fallback pass added the GPT Image-generated Mina photo strip and replaced abstract Blink preview frames with a readable 3-frame person strip in `MY` and generic `BlinkStrip` fallback UI. Verification: `npm.cmd run typecheck` passed, `npm.cmd test -- --runInBand` passed 240 tests, `npx.cmd --yes expo-doctor` passed 17/17, `git diff --check` passed with CRLF warnings only, Pixel_7 emulator rendered the updated `MY` widget preview and pack detail, and logcat crash/error filters stayed clean. Screenshots: `.omx/state/blink-person-model-my-v2.png`, `.omx/state/blink-person-model-detail.png`.
- 2026-05-10: Blink video-frame sample correction on branch `codex/blink-video-frame-sample` replaced the prior sample crop with a timestamped 2-second Blink strip whose middle frame keeps both arms inside the frame. Verification: `npm.cmd test -- --runInBand blinkPersonModel` passed, `npm.cmd run typecheck` passed, full `npm.cmd test -- --runInBand` passed 240 tests, `npx.cmd --yes expo-doctor` passed 17/17, `git diff --check` passed with CRLF warnings only, Android export/embed passed, Pixel_7 emulator rendered the corrected `MY` Blink preview, and logcat crash/error filters stayed clean. Screenshots: `.omx/state/blink-video-frame-sample-my.png`, `.omx/state/blink-video-frame-sample-detail.png`.
- 2026-05-10: Blink color-frame correction on branch `codex/blink-color-video-frames` replaced the monochrome-looking sample with a full-color 2-second video-frame strip while preserving timestamps and visible middle-frame arms. Verification: `npm.cmd test -- --runInBand blinkPersonModel` passed, `npm.cmd run typecheck` passed, full `npm.cmd test -- --runInBand` passed 240 tests, `npx.cmd --yes expo-doctor` passed 17/17, `git diff --check` passed with CRLF warnings only, Android export/embed passed, Pixel_7 emulator rendered the corrected full-color `MY` Blink preview, and logcat crash/error filters stayed clean. Screenshot: `.omx/state/blink-color-video-frames-detail.png`.
- 2026-05-11: Lazyweb/runtime mockup UI direction was applied to the real app in a scoped pass: app typography now uses Pretendard for UI/Korean text and IBMPlexMono for codes/metadata, Today `WIDGET MIRROR` now resembles the actual Android medium widget with action chips and recent senders; Send imports the mockup `Signal Deck` hero/order with mode, slot deck, recent combo, and no-friend fallback; People imports the mockup `Close Circuit` hero, compact Beep ID card, and functional `WIDGET CIRCLE`; `MY` keeps the stronger app-native room/profile/widget-preview flow and only retains a small `Widget Control` utility strip; and Send/Friend picker density was tightened. Verification: `npm.cmd run typecheck` passed, focused Today/Send/People Jest tests passed, full `npm.cmd test -- --runInBand` passed 240 tests, `npx.cmd --yes expo-doctor` passed 17/17, and `git diff --check` passed with CRLF warnings only. Android Pixel_7 UI Preview was rebuilt, installed, and smoke-checked for Today/People/Send; Today screenshot after the placeholder fix is `C:/Users/slime/AppData/Local/Temp/beepget-today-v3.png`; follow-up Send/People captures after importing the mockup structure are `C:/Users/slime/AppData/Local/Temp/beepget-send-mockup-import-v2.png` and `C:/Users/slime/AppData/Local/Temp/beepget-people-mockup-import-v2.png`; `MY` was then corrected back toward the stronger app-native layout. iOS visual QA was not run in this pass.
- 2026-05-14: iOS TestFlight startup follow-up on PR #48 added an iOS Google login fallback beside Apple, inline login/profile error feedback on the Auth slip, a font-load fallback so the splash screen does not stay up forever on font failures, Beepy mascot-based app/adaptive/splash icons, unique 20260514 icon/splash asset paths, and explicit `expo-splash-screen` config-plugin settings. Build 5 proved PR source reached EAS (`commit 67bf8a1`, new fingerprint), but real-device TestFlight later showed no iOS analytics crash log for Beep Get, pointing at native launch/splash configuration or installed-build identity rather than a normal JS crash.
- 2026-05-14: Isolated debugging of the iOS no-log splash hang found a stronger pre-render fatal candidate: `modules/beep-widget` called `requireNativeModule("BeepWidget")` at import time through stores loaded by `App.tsx`, so a missing/misregistered iOS native module could stop JS before the startup timeout/error boundary. PR #48 now lazy-loads that native module at widget call sites, no-ops safely when unavailable, adds regression coverage for the missing-native-module path, and flattens the iOS app icon PNGs to opaque RGB/no-alpha assets.
- 2026-05-14: Real-device iOS build 8 opened successfully, then exposed follow-up UX/runtime issues: the invite share copy was mojibake and too raw, the widget entry opened a debug-style `WidgetStates` modal that clipped under iOS modal chrome, the iOS Google fallback sent users to a Supabase `Unsupported provider` web error, and stale profile state could skip nickname setup. Branch `codex/ios-runtime-ux-fixes` replaces the widget debug modal with a `Widget Setup` screen, makes the modal full-screen, removes the iOS Google fallback from the UI, resets stale profile state on new sessions, and rewrites invite copy to readable ASCII.
- 2026-05-14: Production pager goal resumed on branch `codex/production-pager-goal`. iOS widget source now carries Beep/Blink kind, teaser, and `OK / 8282 / OPEN` deep-link actions; the config plugin copies shared widget data into both iOS extension source folders; People now uses a functional `Widget Circle` instead of leftover circuit-map language; in-app widget previews now match native widget wording (`Incoming Blink`, `Mina - NO 04`, action chips). Verification passed: `npm.cmd test -- --runInBand` (248 tests), `npm.cmd run typecheck`, `npx.cmd --yes expo-doctor` (17/17), `git diff --check` (CRLF warnings only), Android export/embed, `android\gradlew.bat -p android :app:assembleDebug -PreactNativeArchitectures=x86_64 --console=plain --no-daemon`, emulator tap QA through login UI Preview -> People -> Widget Setup, and logcat showed no `FATAL EXCEPTION`.
- Current provider audit: linked Supabase project is `beep-get-prod` (`dyuzxilukcwiavtvbmci`). Local `supabase/config.toml` still has Apple provider disabled and has no Google provider block; do not `supabase config push` this local file to production because provider secrets/dashboard settings are external. Apple provider must be enabled in Supabase dashboard or via Management API with Apple Services ID and generated secret.
- 2026-05-14: iOS extension target-generation pass started on branch `codex/ios-widget-target-generation`. Added `@bacons/apple-targets`, explicit `ios.appleTeamId`, app-group entitlements in `app.json`, and committed `targets/BeepWidgetExtension` plus `targets/BeepNotificationService` so EAS prebuild receives `com.hypeboyo.beepget.widget` and `com.hypeboyo.beepget.notificationservice` app extensions sharing `group.com.beepget.shared`. Package overrides keep the build-time target plugin on compatible `@bacons/xcode`, `@expo/plist`, and `@xmldom/xmldom` versions; root `@xmldom/xmldom@0.8.13` is intentionally present because `@bacons/xcode` imports it directly, and `0.9.x` breaks `@expo/plist` parsing by requiring an explicit mime type. Verification passed: `expo config --type prebuild` showed both `appExtensions` entries, `npm.cmd test -- --runInBand` passed 249 tests, `npm.cmd run typecheck` passed, `npx.cmd --yes expo-doctor` passed 17/17, `npm.cmd audit --audit-level=high` passed with only low/moderate remaining, `git diff --check` passed with CRLF warnings only, Android export/embed passed, and `android\gradlew.bat -p android :app:assembleDebug -PreactNativeArchitectures=x86_64 --console=plain --no-daemon` passed. Windows Expo cannot generate iOS native project files, so `.xcodeproj`/`.appex` inspection still needs macOS or EAS build output.
- Known gap: Medium Android launcher widget action-chip placement, real authenticated Android camera/file upload against Supabase Storage, production thumbnail/3-frame extraction for real Blink captures, Apple OAuth provider dashboard configuration, Play Console configuration, and real iOS widget placement verification remain pending.
- 2026-05-14: Android widget/reply goal pass on branch `codex/android-widget-reply-goal` made Widget Setup Android-first on Android, queued widget action deep links until navigation/auth/profile are ready, and routes completed widget confirm/save/quick-reply actions into Reply Room. Verification passed: focused widget/deep-link/message tests passed, `npm.cmd run typecheck` passed, full `npm.cmd test -- --runInBand` passed 255 tests, `npx.cmd --yes expo-doctor` passed 17/17, `git diff --check` passed with CRLF warnings only, Android export/embed passed, `android\gradlew.bat -p android :app:assembleDebug -PreactNativeArchitectures=x86_64 --console=plain --no-daemon` passed, `Pixel_7` emulator UI Preview opened Today, `beepget://signal/preview-message-1/quick-reply/8282` delivered into the running app and showed Reply Room `READ`, `beepget://widget-states` showed Android setup copy, Android widget providers were visible in `dumpsys appwidget`, and logcat checks stayed free of `FATAL EXCEPTION` / `Widget action failed`.
- 2026-05-14: Revenue/push/cleanup foundation started on branch `codex/revenue-push-cleanup-foundation`. The app now includes Expo IAP dependencies and an identity-pack purchase boundary through `verify-iap`, but production entitlement grants remain disabled until App Store / Play receipt verification secrets are configured; only `IAP_VERIFICATION_MODE=passthrough-for-internal-testing` should unlock packs for internal QA. Expo push token registration writes to `push_tokens`, Beep/reply sends trigger `send-signal-push`, and expired unsaved Blink media can be marked/removed through `cleanup-expired-signals` with `CLEANUP_SHARED_SECRET`. Verification passed: `npm.cmd run typecheck`, full `npm.cmd test -- --runInBand` passed 259 tests, `npx.cmd --yes expo-doctor` passed 17/17 after aligning `expo-constants` to SDK 54, `npx.cmd expo prebuild --platform android --no-install` passed without native file diffs, and `npm.cmd run supabase:dry-run` showed pending migrations `20260510150000_allow_text_signal_tokens.sql` plus `20260514190000_revenue_push_cleanup_foundation.sql`. `npm.cmd run supabase:lint` could not run because the local Supabase Postgres port `127.0.0.1:54322` was not running.
- 2026-05-14: iOS auth/StoreKit setup follow-up added `docs/deploy/ios-auth-storekit.md`, Apple client-secret/provider/secrets/IAP creation scripts, and strict-mode iOS App Store Server API transaction lookup in `verify-iap`. Remaining account-side setup still requires Apple Developer/App Store Connect values that must stay out of git: Sign in with Apple `.p8` signing key for Supabase Apple native token verification, App Store Connect non-consumable products for the four identity pack IDs, and App Store Server API key secrets in Supabase Edge Functions.
- 2026-05-14: Native iOS Apple login follow-up switched iOS from Supabase OAuth redirect to `expo-apple-authentication` plus `supabase.auth.signInWithIdToken({ provider: "apple" })`, while Android/web continue using Google OAuth/PKCE. `app.json` now enables `ios.usesAppleSignIn` and the `expo-apple-authentication` config plugin. Apple fullName is copied to Supabase user metadata when Apple returns it, but login does not fail if that optional metadata update fails. Supabase Apple provider setup should now use the native bundle ID `com.hypeboyo.beepget` as client id unless a separate web OAuth fallback is intentionally added. Verification passed: `npm.cmd run typecheck`, full `npm.cmd test -- --runInBand` passed 263 tests, `npx.cmd --yes expo-doctor` passed 17/17, `npx.cmd expo prebuild --platform android --no-install` completed with no package changes, `npm.cmd audit --audit-level=high` passed with only low/moderate Expo/Jest transitive findings, script syntax checks passed for Apple helper scripts, and `git diff --check` passed.
- 2026-05-14: Final-build readiness audit was captured at `docs/qa/final-build-readiness-2026-05-14.md` for iOS and Android. Code gates are green, but final production builds remain blocked by remote Supabase migrations/functions/secrets, Supabase Apple provider enablement, iOS extension provisioning, StoreKit/Play product setup, and real two-device QA.
- 2026-05-14: Supabase production deploy checkpoint: remote migrations are now applied through `20260514190000_revenue_push_cleanup_foundation.sql`, `npm.cmd run supabase:dry-run` reports the remote database is up to date, and remote Edge Functions `verify-iap`, `send-signal-push`, and `cleanup-expired-signals` are ACTIVE alongside `delete-account`. Remote secrets now include `APP_BUNDLE_ID` and `CLEANUP_SHARED_SECRET`. Supabase Apple provider enablement still needs a Supabase Management API access token or dashboard action; App Store Server API secrets still need real App Store Connect API key values.
- 2026-05-14: Apple account setup report received: App ID `com.hypeboyo.beepget` has Sign in with Apple enabled, Services ID `com.hypeboyo.beepget.signin` is configured for `dyuzxilukcwiavtvbmci.supabase.co` with callback `https://dyuzxilukcwiavtvbmci.supabase.co/auth/v1/callback`, Sign in with Apple key `98ZCRX9Y55` exists, and the four App Store Connect non-consumable identity-pack products exist as drafts. The Supabase provider script/docs now use provider client IDs `com.hypeboyo.beepget.signin,com.hypeboyo.beepget` while signing the Apple client-secret JWT with Services ID `com.hypeboyo.beepget.signin`.
- 2026-05-15: Supabase Apple provider was enabled on `beep-get-prod` via Management API. Verified safe auth config fields: `external_apple_enabled=true`, `external_apple_client_id=com.hypeboyo.beepget.signin,com.hypeboyo.beepget`, and an Apple secret is present. Keep issue `#53` open until real iOS device QA confirms Apple login reaches nickname/profile flow.
- 2026-05-15: App Store Server API secrets were set on `beep-get-prod` for Beep Get IAP verification using key ID `JQ6KM739V7` and `APP_BUNDLE_ID=com.hypeboyo.beepget`; `verify-iap` was redeployed afterward and remote function list shows it ACTIVE. The earlier key ID `824QSV86HA` was revoked and must not be used. Remaining IAP launch work is App Store Connect product price/availability/localization/review screenshots plus real sandbox purchase QA.
- 2026-05-15: iOS extension provisioning blocker `#52` was resolved. EAS remote credentials now include active provisioning profiles for `com.hypeboyo.beepget` (`42MX84F7SS`), `com.hypeboyo.beepget.widget` (`L5B96HY99Q`), and `com.hypeboyo.beepget.notificationservice` (`TUCHMV2GT3`) with the shared distribution certificate `5BC2FCABB90D7CE86A3ACB99329C50DB`. Follow-up iOS build blockers were also fixed: `a6356f92-f6ae-4856-aa16-c9f48ee449fe` exposed the `@expo/plist`/`@xmldom/xmldom` Prebuild compatibility issue, `5a3b973b-596e-433e-85c0-5f696cf737d5` exposed iOS widget Swift compatibility issues, and production iOS build `b8a22a81-7cf4-43ca-b77f-fae108e2c25e` finished successfully for `1.0.0 (16)` with IPA `https://expo.dev/artifacts/eas/oNmWgM4mtgAJAB4ykuTQuN.ipa`.

## Related Vault Notes

- `C:/Users/slime/claude-projects/Obsidian Vault/Projects/beep-get/`

## Handoff Rule

When work changes navigation, native modules, Supabase behavior, visual system, or platform support, update this file with the new status and next concrete action.
